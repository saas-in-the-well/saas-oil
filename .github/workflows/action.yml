name: Java CI with Gradle

on:
  push:
    branches: [ "dev/dev_anseongjin" ]
  pull_request:
    branches: [ "dev/dev_anseongjin" ]

permissions:
  id-token: write   # This is required for requesting the JWT

env:
  ecr_url: 794187215716.dkr.ecr.ap-northeast-2.amazonaws.com/saas-oil
  role_arn: arn:aws:iam::794187215716:role/saas-git-action
  aws_region: ap-northeast-2

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up JDK 11
        uses: actions/setup-java@v4
        with:
          java-version: '11'
          distribution: 'temurin'
          cache: maven

      - name: Build with Maven
        run: mvn -B package --file pom.xml

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: saas-oil-application-jar
          path: target/*.jar

      - name: List files in the working directory - 1
        run: ls target/

  docker:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: saas-oil-application-jar
          path: target/

      - name: configure aws credentials
        uses: aws-actions/configure-aws-credentials@v3
        with:
          role-to-assume: ${{ env.role_arn }}
          role-session-name: ecrRoleSession
          aws-region: ${{ env.aws_region }}

      # Docker 이미지 빌드
      - name: Build Docker image
        run: docker build . -t ${{ env.ecr_url }}:tag-${{ github.sha }}

      # 이미지를 ECR에 푸시
      - name: Push Docker image to AWS ECR
        run: docker push ${{ env.ecr_url }}:tag-${{ github.sha }}